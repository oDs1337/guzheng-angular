import { DateTransformationPipe } from './date-transformation.pipe';

describe('DateTransformationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransformationPipe();
    expect(pipe).toBeTruthy();
  });
});
